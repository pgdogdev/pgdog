# ------------------------------------------------------------------------------
# ----- General ----------------------------------------------------------------

[general]
auth_type = "trust"

# ------------------------------------------------------------------------------
# ----- Database :: pgdog ------------------------------------------------------

[[databases]]
name = "pgdog"
host = "/tmp"
port = 5432
database_name = "pgdog"
user = "pgdog"
