[package]
name = "test-plugin-compatible"
version = "0.1.0"
edition = "2024"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
pgdog-plugin = { path = "../../../../pgdog-plugin" }
assertions = { path = "../assertions" }
